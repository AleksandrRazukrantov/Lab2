## Отчет по лабораторной работе № 2

#### № группы: `ПМ-2501`

#### Выполнил: `Разукрантов Александр Евгеньевич`

#### Вариант: `18`

### Cодержание:

- [Задание 1](#1-Задание-1)
- [Заданеи 2](#2-Заданеи-2)
- [Задание 3](#3-Заданеи-3)
- [Задание 4](#4-Заданеи-4)

- [Постановка задачи](#1-постановка-задачи)
- [Входные и выходные данные](#2-входные-и-выходные-данные)
- [Алгоритм](#3-алгоритм)
- [Программа](#4-программа)
- [Анализ правильности решения](#5-анализ-правильности-решения)
### 1. Задание 1
- [Постановка задачи 1](#1-постановка-задачи-1)
- [Входные и выходные данные 1](#2-входные-и-выходные-данные-1)
- [Алгоритм 1](#3-алгоритм-1)
- [Программа 1](#4-программа-1)
- [Анализ правильности решения 1](#5-анализ-правильности-решения-1)
### 1. Постановка задачи 1

> Программа получает на вход 4 числа X, A, B и C. Нужно проверить какие из грузов А, В и С можно поместить в лифт грузоподъемностью Х, а также
> найти максимальное количество грузов которое мы можем поместить в лифт.
> Данную задачу можно разделить на 2 подзадачи:  Нужно проверить какие А, В и С меньше или равны Х, найти какие суммы А,В и С((А+В),(В+С),(А+С),(А+В+С)) меньшее или равны Х.

- Для 1 подзадачи нужно рассмотреть 3 случая:
    1. `X >= А` (Поподает ли груз А в лифт)
    2. `X >= В` (Поподает ли груз В в лифт)
    3. `X >= С` (Поподает ли груз С в лифт)
- Для 2 подзадачи нужно рассмотреть 4 случая:
    1. `Х >= (А+В)`
    2. `Х >= (А+С)`
    3. `Х >= (С+В)`
    4. `Х >= (А+С+В)`

Всего надо рассмотреть `15` случаев.

### 2. Входные и выходные данные 1

#### Данные на вход



#### Данные на выход

Т.к  программа должна вывести какие грузы можно поместить в лифт(т.е проверить какие А, В и С меньше или равны Х), то на выход 
мы получим строку тех грузов(А,В и С) которые входят в лифт(меньше или равны Х). Также, т.к нам нужно будет получить сколько 
максимально грузов мы можем поместить в лифт(проверить какие суммы А,В и С((А+В),(В+С),(А+С),(А+В+С)) меньшее Х), то на следующей строке на выход будет число 1,2 или 3.

### 3. Алгоритм 1

#### Алгоритм выполнения программы:

1. **Ввод данных:**  
   Программа считывает четыре натуральных числа, обозначенные как `Х`,`А`,`В` и `С`.

2. **Сравнение чисел:**  
   Программа сравнивает значения `Х` и `А`. Если `Х` больше или равно `А`, программа выводит А(Если нет, то ничего не выводит) и переходит к следующему шагу:
   сравнение `Х` и `В`. Если `Х` больше или равно `В`, программа выводит В(Если нет, то ничего не выводит) и переходит к следующему шагу:
   сравнение `Х` и `С`. Если `Х` больше или равно `С`, программа выводит С(Если нет, то ничего не выводит). Если не один из элементов не превосходит Х, то выводится пустая строка.

4. **Поиск максимального количества элементов:**
    - Если после сравнения один или ноль грузов были меньше или равны Х, то программа выводит 1 или 0 соответственно.
    - Если после сравнения два груза были меньше или равны Х, то программа проверяет меньше или равна их сумма Х(иксу). Если да, то выводит 2, иначе выводит 1
    - Если после сравнения три груза были меньше или равны Х, то программа проверяет меньше или равна их сумма Х(иксу). Если да, то выводит 3, иначе программа начинает сравнивать их
      попарные суммы. Если хотябы одна сумма оказалась меньше или равна Х, то выводит 2, иначе выводит 1

5. **Вывод результата:**  
   На экран выводится две строки:
   1. Либо строка состоящая из А,В или С, либо пустая строка
   2. Число максимального количества грузов, которое можно поместить в лифт


### 4. Программа 1

```import java.util.Scanner;

```

### 5. Анализ правильности решения 1

Программа работает корректно на всем множестве решений с учетом ограничений.

1. Тест на `X < A < B < C`:

    - **Input**:
        ```
        5 6 7 8
        ```

    - **Output**:
        ```

        0
        ```
### 2. Задание 2
- [Постановка задачи 2](#1-постановка-задачи-2)
- [Входные и выходные данные 2](#2-входные-и-выходные-данные-2)
- [Алгоритм 2](#3-алгоритм-2)
- [Программа 2](#4-программа-2)
- [Анализ правильности решения 2](#5-анализ-правильности-решения-2)
### 1. Постановка задачи 2

> Программа получает на вход 4 числа X, A, B и C. Нужно проверить какие из грузов А, В и С можно поместить в лифт грузоподъемностью Х, а также
> найти максимальное количество грузов которое мы можем поместить в лифт.
> Данную задачу можно разделить на 2 подзадачи:  Нужно проверить какие А, В и С меньше или равны Х, найти какие суммы А,В и С((А+В),(В+С),(А+С),(А+В+С)) меньшее или равны Х.

- Для 1 подзадачи нужно рассмотреть 3 случая:
    1. `X >= А` (Поподает ли груз А в лифт)
    2. `X >= В` (Поподает ли груз В в лифт)
    3. `X >= С` (Поподает ли груз С в лифт)
- Для 2 подзадачи нужно рассмотреть 4 случая:
    1. `Х >= (А+В)`
    2. `Х >= (А+С)`
    3. `Х >= (С+В)`
    4. `Х >= (А+С+В)`

Всего надо рассмотреть `15` случаев.

### 2. Входные и выходные данные 2

#### Данные на вход



#### Данные на выход

Т.к  программа должна вывести какие грузы можно поместить в лифт(т.е проверить какие А, В и С меньше или равны Х), то на выход 
мы получим строку тех грузов(А,В и С) которые входят в лифт(меньше или равны Х). Также, т.к нам нужно будет получить сколько 
максимально грузов мы можем поместить в лифт(проверить какие суммы А,В и С((А+В),(В+С),(А+С),(А+В+С)) меньшее Х), то на следующей строке на выход будет число 1,2 или 3.

### 3. Алгоритм 2

#### Алгоритм выполнения программы:

1. **Ввод данных:**  
   Программа считывает четыре натуральных числа, обозначенные как `Х`,`А`,`В` и `С`.

2. **Сравнение чисел:**  
   Программа сравнивает значения `Х` и `А`. Если `Х` больше или равно `А`, программа выводит А(Если нет, то ничего не выводит) и переходит к следующему шагу:
   сравнение `Х` и `В`. Если `Х` больше или равно `В`, программа выводит В(Если нет, то ничего не выводит) и переходит к следующему шагу:
   сравнение `Х` и `С`. Если `Х` больше или равно `С`, программа выводит С(Если нет, то ничего не выводит). Если не один из элементов не превосходит Х, то выводится пустая строка.

4. **Поиск максимального количества элементов:**
    - Если после сравнения один или ноль грузов были меньше или равны Х, то программа выводит 1 или 0 соответственно.
    - Если после сравнения два груза были меньше или равны Х, то программа проверяет меньше или равна их сумма Х(иксу). Если да, то выводит 2, иначе выводит 1
    - Если после сравнения три груза были меньше или равны Х, то программа проверяет меньше или равна их сумма Х(иксу). Если да, то выводит 3, иначе программа начинает сравнивать их
      попарные суммы. Если хотябы одна сумма оказалась меньше или равна Х, то выводит 2, иначе выводит 1

5. **Вывод результата:**  
   На экран выводится две строки:
   1. Либо строка состоящая из А,В или С, либо пустая строка
   2. Число максимального количества грузов, которое можно поместить в лифт


### 4. Программа 2

```import java.util.Scanner;

```

### 5. Анализ правильности решения 2

Программа работает корректно на всем множестве решений с учетом ограничений.

1. Тест на `X < A < B < C`:

    - **Input**:
        ```
        5 6 7 8
        ```

    - **Output**:
        ```

        0
        ```
### 3. Задание 3
- [Постановка задачи](#1-постановка-задачи)
- [Входные и выходные данные](#2-входные-и-выходные-данные)
- [Алгоритм](#3-алгоритм)
- [Программа](#4-программа)
- [Анализ правильности решения](#5-анализ-правильности-решения)
### 4. Задание 4
- [Постановка задачи](#1-постановка-задачи)
- [Входные и выходные данные](#2-входные-и-выходные-данные)
- [Алгоритм](#3-алгоритм)
- [Программа](#4-программа)
- [Анализ правильности решения](#5-анализ-правильности-решения)

### 1. Постановка задачи

> Программа получает на вход 4 числа X, A, B и C. Нужно проверить какие из грузов А, В и С можно поместить в лифт грузоподъемностью Х, а также
> найти максимальное количество грузов которое мы можем поместить в лифт.
> Данную задачу можно разделить на 2 подзадачи:  Нужно проверить какие А, В и С меньше или равны Х, найти какие суммы А,В и С((А+В),(В+С),(А+С),(А+В+С)) меньшее или равны Х.

- Для 1 подзадачи нужно рассмотреть 3 случая:
    1. `X >= А` (Поподает ли груз А в лифт)
    2. `X >= В` (Поподает ли груз В в лифт)
    3. `X >= С` (Поподает ли груз С в лифт)
- Для 2 подзадачи нужно рассмотреть 4 случая:
    1. `Х >= (А+В)`
    2. `Х >= (А+С)`
    3. `Х >= (С+В)`
    4. `Х >= (А+С+В)`

Всего надо рассмотреть `15` случаев.

### 2. Входные и выходные данные

#### Данные на вход



#### Данные на выход

Т.к  программа должна вывести какие грузы можно поместить в лифт(т.е проверить какие А, В и С меньше или равны Х), то на выход 
мы получим строку тех грузов(А,В и С) которые входят в лифт(меньше или равны Х). Также, т.к нам нужно будет получить сколько 
максимально грузов мы можем поместить в лифт(проверить какие суммы А,В и С((А+В),(В+С),(А+С),(А+В+С)) меньшее Х), то на следующей строке на выход будет число 1,2 или 3.

### 3. Алгоритм

#### Алгоритм выполнения программы:

1. **Ввод данных:**  
   Программа считывает четыре натуральных числа, обозначенные как `Х`,`А`,`В` и `С`.

2. **Сравнение чисел:**  
   Программа сравнивает значения `Х` и `А`. Если `Х` больше или равно `А`, программа выводит А(Если нет, то ничего не выводит) и переходит к следующему шагу:
   сравнение `Х` и `В`. Если `Х` больше или равно `В`, программа выводит В(Если нет, то ничего не выводит) и переходит к следующему шагу:
   сравнение `Х` и `С`. Если `Х` больше или равно `С`, программа выводит С(Если нет, то ничего не выводит). Если не один из элементов не превосходит Х, то выводится пустая строка.

4. **Поиск максимального количества элементов:**
    - Если после сравнения один или ноль грузов были меньше или равны Х, то программа выводит 1 или 0 соответственно.
    - Если после сравнения два груза были меньше или равны Х, то программа проверяет меньше или равна их сумма Х(иксу). Если да, то выводит 2, иначе выводит 1
    - Если после сравнения три груза были меньше или равны Х, то программа проверяет меньше или равна их сумма Х(иксу). Если да, то выводит 3, иначе программа начинает сравнивать их
      попарные суммы. Если хотябы одна сумма оказалась меньше или равна Х, то выводит 2, иначе выводит 1

5. **Вывод результата:**  
   На экран выводится две строки:
   1. Либо строка состоящая из А,В или С, либо пустая строка
   2. Число максимального количества грузов, которое можно поместить в лифт


### 4. Программа

```import java.util.Scanner;

public class Main {
    // Объявляем объект класса Scanner для ввода данных
    public static Scanner scan = new Scanner(System.in);

    public static void main(String[] args) {
        // Считывание четерех натуральных чисел Х,А,В и С консоли
        int x = scan.nextInt();
        int a = scan.nextInt();
        int b = scan.nextInt();
        int c = scan.nextInt();
        // Определение можно ли поместить груз А в лифт
        // Если да, выводи А
        if (x >= a) {
            System.out.print("A ");
            // Определение можно ли поместить груз В в лифт
            // Если да, выводи В
            if (x >= b) {
                System.out.print("B ");
                // Определение можно ли поместить груз С в лифт
                // Если да, выводи С
                // Перехим на новую строку для поиска максимального
                if (x >= c) {
                    System.out.println("C");
                    // Определение можно ли поместить три груза в лифт
                    // Если да, выводи 3
                    if (x >= a + b + c) {
                        System.out.println("3");

                    } 
                    else {
                        // Определение можно ли поместить два груза в лифт
                        // Если да, выводи 2
                        if (x >= a + b) {
                            System.out.println("2");
                        } 
                        else {
                            // Определение можно ли поместить два груза в лифт
                            // Если да, выводи 2
                            if (x >= a + c) {
                                System.out.println("2");
                            } 
                            else {
                                // Определение можно ли поместить два груза в лифт
                                // Если да, выводи 2
                                if (x >= c + b) {
                                    System.out.println("2");
                                } 
                                else {
                                    // Если нет выводим 1
                                    System.out.println("1");
                                }
                            }
                        }
                    }


                } 
                else {
                    // Если С больше Х
                    // Перехим на новую строку для поиска максимального
                    System.out.println();
                    // Определение можно ли поместить два груза в лифт
                    // Если да, выводим 2
                    if (x >= b + a) {
                        System.out.println("2");
                    } 
                    else {
                        // Если нет, выводим 1
                        System.out.println("1");
                    }
                }
            } 
            else {
                // Если В больше Х
                // Определение можно ли поместить груз С в лифт
                // Если да, выводим С
                // Перехим на новую строку для поиска максимального
                if (x >= c) {
                    System.out.println("C");
                    // Определение можно ли поместить два груза в лифт
                    // Если да, выводим 2
                    if (x >= c + a) {
                        System.out.println("2");
                    } 
                    else {
                        // Если нет, выводим 1
                        System.out.println("1");
                    }
                } 
                else {
                    // Если В и С больше Х
                    // Выводим 1
                    System.out.println();
                    System.out.println("1");
                }


            }
        } 
        else {
            // Если А больше Х
            // Определение можно ли поместить груз В в лифт
            // Если да, выводим В
            if (x >= b) {
                System.out.print("B ");
                // Определение можно ли поместить груз С в лифт
                // Если да, выводим С
                // Перехим на новую строку для поиска максимального
                if (x >= c) {
                    System.out.println("C");
                    if (x >= b + c) {
                        System.out.println("2");
                    } 
                    else {
                        System.out.println("1");
                    }
                } 
                else {
                    // Если А и С больше Х
                    // Выводим 1
                    System.out.println();
                    System.out.println("1");
                }
            } 
            else {
                // Если А больше Х
                // Если В больше Х
                // Определение можно ли поместить груз С в лифт
                // Если да, выводим С и выводим 1
                if (x >= c) {
                    System.out.println("C");
                    System.out.println("1");
                } 
                else {
                    // Если А,В и С больше Х
                    // Выводим 0
                    System.out.println();
                    System.out.println("0");
                }
            }
        }
    }
}
```

### 5. Анализ правильности решения

Программа работает корректно на всем множестве решений с учетом ограничений.

1. Тест на `X < A < B < C`:

    - **Input**:
        ```
        5 6 7 8
        ```

    - **Output**:
        ```

        0
        ```

2. Тест на `C <= X < A < B`:

    - **Input**:
        ```
        5 6 7 2
        ```

    - **Output**:
        ```
        C
        1
        ```

3. Тест на `B <= X < C < B`:

    - **Input**:
        ```
        5 6 2 7
        ```

    - **Output**:
        ```
        B
        1
        ```

4. Тест на `C < B <= X < A` и `C + B <= X `:

    - **Input**:
        ```
        5 6 2 1
        ```

    - **Output**:
        ```
        B C
        2
        ```

5. Тест на `C < B <= X < A` и `C + B > X `:

    - **Input**:
        ```
       5 6 4 3
        ```

    - **Output**:
        ```
        B C
        1
        ```
6. Тест на `A <= X < B < C`:

    - **Input**:
        ```
        5 4 7 8
        ```

    - **Output**:
        ```
        A
        1
        ```
7. Тест на `A < C <= X < B` и `C + A <= X `:

    - **Input**:
        ```
        5 4 7 1
        ```

    - **Output**:
        ```
        A C
        2
        ```
8. Тест на `A < C <= X < B` и `C + A > X `:

    - **Input**:
        ```
        5 4 7 2
        ```

    - **Output**:
        ```
        A C
        1
        ```
9. Тест на `A < B <= X < C` и `B + A <= X `:

    - **Input**:
        ```
        5 4 1 7
        ```

    - **Output**:
        ```
        A B
        2
        ```
10. Тест на `A < B <= X < B` и `B + A > X `:

    - **Input**:
        ```
        5 4 2 7
        ```

    - **Output**:
        ```
        A C
        1
        ```
11. Тест на `A < C < B <= X` и `C + A  + B <= X `:

    - **Input**:
        ```
        6 1 2 3
        ```

    - **Output**:
        ```
        A B C
        3
        ```
12. Тест на `A < C < B <= X` и `C + A  + B > X ` и `B + A <= X `:

    - **Input**:
        ```
        6 1 2 6
        ```

    - **Output**:
        ```
        A B C
        2
        ```
13. Тест на `A < C < B <= X` и `C + A  + B > X ` и `C + A <= X `:

    - **Input**:
        ```
        6 1 6 4
        ```

    - **Output**:
        ```
        A B C
        2
        ```
14. Тест на `A < C < B <= X` и `C + A  + B > X ` и `B + C <= X `:

    - **Input**:
        ```
        6 6 2 4
        ```

    - **Output**:
        ```
        A B C
        2
        ```
15. Тест на `A < C < B <= X` и `C + A  + B > X ` и `B + A > X `и `C + A > X ` и `B + C > X `:

    - **Input**:
        ```
        6 5 4 3
        ```

    - **Output**:
        ```
        A B C
        1
        ```
(Было рассмотрено все 15 случаев)
